import { SelectLecturersDirective } from './select-lecturers.directive';

describe('SelectLecturersDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectLecturersDirective();
    expect(directive).toBeTruthy();
  });
});
