import { LecturerDepartmentFilterPipe } from './lecturer-department-filter.pipe';

describe('LecturerDepartmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LecturerDepartmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
