import { Department } from './department';

export class Course {

    // constructor(private _name: string, private _goal: string,
    //             private _status: string, private espb: number,
    //             private _department: Department, 
    //             private _studyProgram: StudyProgram)
}
