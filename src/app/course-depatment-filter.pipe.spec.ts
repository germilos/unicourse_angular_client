import { CourseDepatmentFilterPipe } from './course-depatment-filter.pipe';

describe('CourseDepatmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseDepatmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
